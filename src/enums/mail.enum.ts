export enum MailTypes {
  verification = "verification",
  resetPassword = "reset-password",
}

export enum MailText {
  verification = "Please click at link for verification you email",
  resetPassword = "Please click at link for resetting you password",
}
